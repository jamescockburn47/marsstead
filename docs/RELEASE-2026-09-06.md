# Web playtest release — 6 September 2026

Authorised by James: apply all current updates live and deployed for a family web playtest.

Includes the supplied opening home, workers and rover outing; inhabited base/activities/workings; astronaut gait and procedural surface refinements; night/storm preparation and cleanup; dynamic VESPER context, quiet delivery, consistent voice and typed/spoken conversation; and an explicit player feedback form backed by the existing Mars ledger.

The canonical website path is `npm run deploy`, after committing and pushing the verified source on main. Local `.superpowers/` design-server scratch and rejected `prototypes/` are preserved but excluded from source/release upload. No saved game is reset or replaced.

## Recovery

The previous Vercel deployment is `https://marsstead-qapgtneri-james-cockburns-projects.vercel.app` (`dpl_8ceT9bTKGRy45Dto4v3RSuKQDimU`). Do not blindly return players with new saves to that older client: its serializer drops new opening/weather/activity fields and derives funding from dig progress, so resaving can lose progression or recharge a prepaid passage. Prefer a forward correction preserving the current save schema; preserve/export affected browser saves before testing an old client.

The independently deployed VESPER relay has a five-file source backup at `/home/james/marsstead/relay-backup-20260906`. Restore those files to the same corresponding `brain/src` and `brain/server` paths and restart only `marsstead-brain.service` if rollback is needed. This does not change keys, quotas, game saves, ledger storage, Caddy or the tunnel.

## Feedback

Pause → Playtest feedback. Short written observations are sent only when the player presses Send, to the existing `/dash/feedback` route. No recording, chat transcript or save is attached. The ledger stores the message, coarse context and request IP; it is not anonymous. Public `/api/feedback` is blocked; read the private inbox through EVO loopback port 8098 or the existing board. The server accepts at most eight submissions per IP/day; a few combined observations are preferable to a report for every click.

## Pre-release checks

Canonical verify and build passed on the integrated source. Desktop/phone VESPER conversation checks, actual touch first-use check and feedback success/failure/duplicate/pause checks passed. Fresh feedback review found no material issues. Live website entry and real VESPER chat/TTS are probed after the canonical deployment; disposable evidence is written under `media/release/`.
